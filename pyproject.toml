[project]
name = "ueba-detector"
version = "0.4.0"
description = "Host UEBA anomaly detector and normalized security event collector"
requires-python = ">=3.10"
dependencies = ["psutil>=5.9"]

[project.scripts]
ueba-detector = "ueba_detector.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
